{
  "name": "nutrismart",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma SaaS de gestion nutricional (multi-tenant, white-label)",
  "workspaces": [
    "apps/api",
    "packages/design-system"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @nutrismart/api",
    "migrate": "npm run migrate -w @nutrismart/api",
    "seed": "npm run seed -w @nutrismart/api"
  },
  "engines": {
    "node": ">=20"
  }
}
